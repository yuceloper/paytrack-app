name: paytrack_app
description: PayTrack personal payment tracking app
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  go_router: ^16.2.1
  intl: ^0.20.2
  http: ^1.5.0
  flutter_local_notifications: ^20.1.0
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0
  timezone: ^0.10.1
  shared_preferences: ^2.5.3
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
